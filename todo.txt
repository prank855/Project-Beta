Handshake server/client and assign ids
SyncVar? https://mirror-networking.gitbook.io/docs/guides/synchronization/syncvars
reference sprites instead of readding to queue
sprite depth/z value/height/ordering/layer
sprite bounding boxes if image not loaded
sprite render smooth/point
private systems
mouse input
++batch network packets instead of sending one by one

dictionary<PacketType,anon func OR per case class> instead of switch

optimize sprite rendering !!!! IMPORTANT

